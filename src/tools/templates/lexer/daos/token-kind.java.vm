package $packageName.$daoPackageName;

/**
 * This file is automatically generated by the compiler tools.
 * Do not manually update! (Use the CompilerTools to regenerate.)
 **/
public enum $tokenKind {
    // Special use internal tokens
    #foreach($token in $intrinsics)
    $token.getConstantName(),
    #end

    // Types
    #foreach($token in $literals)
    $token.getConstantName(),
    #end

    // Identifiers
    Identifier,

    // Keywords
    #foreach($token in $keywords)
    $token.getConstantName(),
    #end

    // Operators
    #foreach($token in $operators)
    $token.getConstantName(),
    #end

    // Separators
    #foreach($token in $separators)
    $token.getConstantName(),
    #end
}
