package $packageName.$daoPackageName;

import java.util.Map;
import java.util.HashMap;

/**
 * This file is automatically generated by the compiler tools.
 * Do not manually update! (Use the CompilerTools to regenerate.)
 **/
public class $tableName {
    private static Map<String, Symbol> symbols;

    static {
        symbols = new HashMap<>();

        #foreach($symbol in $symbolicConstants)
        symbols.put("$symbol.getLexeme()", new Symbol($tokenKind.$symbol.getConstantName(), "$symbol.getLexeme()"));
        #end
    }

    public static Symbol symbol(String lexeme, $tokenKind kind) {
        Symbol s = symbols.get(lexeme);

        if (s == null) {
            if (kind == $tokenKind.$bogusName) {
                // this is used to check for a double character operator without entering it
                // into the table
                return null;
            }

            s = new Symbol(kind, lexeme);
            symbols.put(lexeme, s);
        }

        return s;
    }
}